[package]
name = "telemetry-drain"
version = "0.1.0"
edition = "2021"

[dependencies]
libc = "0.2"
arrow = { version = "54", features = ["ipc"] }
prost = "0.13"
tokio = { version = "1", features = ["full"] }
