FROM rust:1.96-alpine AS builder
WORKDIR /src
COPY Cargo.toml Cargo.lock ./
COPY src/ src/
RUN cargo build --release

FROM alpine:3.21
RUN apk add --no-cache ca-certificates
COPY --from=builder /src/target/release/telemetry-drain /telemetry-drain
USER nobody
ENTRYPOINT ["/telemetry-drain"]
